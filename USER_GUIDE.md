# PZ Build 42 伺服器管理器使用手冊

本文件收納進階操作與技術細節。第一次使用只需要先閱讀 [README](README.md) 的安裝步驟；遇到特定問題時再查閱對應章節。

## 1. 安裝與前置導引

### 支援條件

- Windows Server 2022 Desktop Experience x64，或具備 WPF 桌面元件的 64 位元 Windows 10／11。
- 僅支援 Project Zomboid Build 42 Stable。
- 完整設定功能要求 Sandbox `VERSION = 6`；舊版 Sandbox 僅供讀取，不會覆寫。
- Windows 帳號必須能寫入 SteamCMD、PZ Server、PZ 資料與 Manager 設定目錄。

### 第一次啟動

在 SteamCMD 尚未設定前，管理器只顯示前置導引；SteamCMD 完成後才顯示伺服器安裝分類，PZ Server 安裝完成後才開放全部功能。

前置精靈會顯示目前步驟、下載百分比、已執行時間及 SteamCMD 即時輸出。工作期間會鎖定衝突操作，完成或失敗後保留明確結果。

### 連接埠

防火牆與路由器必須允許設定的 UDP 連接埠。遊戲主要連接埠預設為 `16261`，直接連線埠預設為 `16262`；實際值仍以所選 INI 為準。

## 2. 介面與設定檔管理

### 主要頁面

- **儀表板**：伺服器狀態、控制台、在線玩家、立即存檔、備份、啟動與安全關服。
- **伺服器設定**：基礎與進階圖形化設定。
- **設定檔管理**：掃描、建立、複製、選取與檢查完整鍵值。
- **自動化**：定時重啟、玩家查詢、Workshop 更新檢查與公告。
- **診斷與記錄**：環境預檢、GUI 記錄與隱私清理診斷包。

### 建立新設定檔

「新設定檔」會建立乾淨的 Build 42 Stable `VERSION = 6` 設定，不帶入目前 GUI、伺服器名稱、密碼、MOD、世界或玩家資料。

「複製並重新命名」只複製設定內容：

- 不複製世界、角色或玩家資料庫。
- 自動產生新的 `ResetID`。
- 可選擇清除密碼。
- 可避開其他設定檔已使用的連接埠。

### 掃描與載入

管理器會掃描 SteamCMD／PZ 安裝位置及 PZ 資料目錄。選取設定檔後，INI 與 `_SandboxVars.lua` 會固定使用同一組資料目錄及設定檔名稱，避免被舊 Manager 設定切回其他伺服器。

## 3. 讀取、編碼與安全儲存

### 儲存前提

管理器不會因為開啟頁面、切換編碼或修改 GUI 欄位就改寫檔案。只有明確按下儲存後才會寫入，而且必須符合以下條件：

1. 已成功讀取目前檔案。
2. Sandbox 為支援的 Build 42 Stable `VERSION = 6`。
3. 檔案從讀取後沒有被其他程式修改。
4. 輸入值符合可確認的類型、範圍或選項。
5. 文字不含 `�` 或連續 `???` 等明顯不可逆損壞。

### 編碼處理

支援 UTF-8、UTF-8 BOM、Big5／CP950、UTF-16 LE 與 UTF-16 BE。自動模式依序辨識 BOM、嚴格 UTF-8、Big5，不依賴 Windows 顯示語言。

- 讀取到什麼編碼，就以相同編碼儲存。
- 有效 UTF-8 不會被強制當成 Big5。
- 切換編碼只重新解讀，不會直接寫回。
- 「修復 UTF-8 曾被 Big5 誤讀後存檔」只適用於仍可逆的 mojibake；已變成 `?` 或 `�` 的遺失字元必須由備份還原或重新輸入。
- 啟動 PZ 時依所選 INI 的實際編碼設定該程序的 JVM `file.encoding` 與控制台串流，不修改 Windows、HOST、NODE 或 VM 全域語系。

### 寫入與驗證

- 寫入前建立 `.manager-backup`。
- 保留 GUI 未管理的設定、註解及原始編碼。
- 只寫入使用者實際變更的受管理欄位。
- 同一 INI 若存在重複鍵，儲存受管理鍵時保留第一筆並清理同區段重複項，避免 GUI 與遊戲讀到不同值。
- 寫入後直接從磁碟重新讀取，逐欄比對；缺欄位、落盤值不同或重讀值不同都不回報成功。
- 儲存前會顯示待寫入差異摘要，未儲存狀態會保留在 GUI。

## 4. 圖形化設定

設定分為「基礎設定」與「進階設定」，每個大分類最多六個子分類。重點設定直接顯示繁中名稱、目前值、可靠預設、允許範圍／編號選項及原始 Build 42 註解；原始設定鍵仍保留供核對。

### 主要分類

- **伺服器與權限**：公開狀態、玩家上限、PVP、安全屋、派系、登入負載、RCON 與伺服器訊息。
- **世界與物資**：時間、停水停電、經驗倍率、物資倍率、重生與存檔週期。
- **玩家與生存**：飢餓、口渴、疲勞、耐力恢復、營養、受傷、骨折、衣物劣化、多重攻擊、背後受擊、血液量及車禍傷害。
- **殭屍**：ZombieLore、人口倍率、巔峰倍率、巔峰日、重生與分布。
- **進階伺服器**：帳號顯示、地圖玩家可見度、重生、派系、安全屋、PVP 傷害、車速與登入佇列。
- **反作弊**：Build 42 的 AntiCheat 設定與目前值。
- **MOD／地圖**：Workshop、Mod ID、依賴、順序、地圖與重生區域。
- **原始設定檔**：查看仍未圖形化或第三方 MOD 自訂的欄位。

遊戲檔案內沒有可靠預設或上限的設定會明確標示「遊戲未註明」，管理器不會猜測。第三方 MOD 欄位也不會被自行翻譯或虛構規則。

### 出生物品

原生 `SpawnItems` 會套用於每次建立新角色，包括死亡後重建。Build 42 原生設定無法把首次創角與復活物品分開。

物品 ID 只驗證、不自動增刪或替換。範例：

`Base.BaseballBat,Base.WaterBottle,Base.Chocolate`

`Base.WaterBottleFull` 是已移除的 Build 41 ID，管理器會拒絕寫入，但不會擅自替換使用者內容。

## 5. MOD 管理完整流程

### 新增 Workshop

1. 到「伺服器設定 → 基礎設定 → 模組 → Workshop／依賴」。
2. 在新增欄輸入 Workshop 數字 ID。
3. 按「加入並解析」。成功項目加入伺服器來源清單，輸入欄自動清空；失敗項目保留供重試。
4. Steam Required Items 只列為候選，由使用者勾選決定是否加入。
5. 新加入的 Mod ID 不會自動啟用。

新增欄只代表本次輸入，永遠不直接充當 INI 的 `WorkshopItems=`。

### 啟用、停用與移除

- 「伺服器模組」列出目前已加入、已下載或可由本機辨識的來源。
- 勾選／取消勾選只控制待寫入 `Mods=` 的 Mod ID。
- 選取列後，Workshop ID、Mod ID、來源、版本、依賴、作者排序與客戶端判定顯示在右側詳情。
- 「移出選取來源」會先列出受影響的相依 Mod。Workshop 來源會一併移出其專屬 Mod、地圖與重生點；本機 Mod 可獨立移出。
- 移出只更新設定，不會刪除本機下載檔案。
- 停用依賴來源時會停用下游 Mod 及相關地圖／重生點，避免留下失效組合。

不同 Workshop 若提供相同 Mod ID，會分別顯示來源，但只允許一個啟用來源；移出主要來源後，仍存在的替代來源可重新接手。

### 掃描與檢查

- **重新掃描已加入**：只讀取已下載內容與本機快取，不重新查詢全部 Steam 項目。
- **檢查本次新增**：只查詢這次加入的 Workshop，減少等待。
- **重新檢查全部**：重新取得所有 Steam 中繼資料。

Steam 查詢採節流與隨機間隔。遇到 HTTP 429 時依 `Retry-After` 或指數退避重試；持續受限時停止本次後續網路查詢並沿用成功快取，不把既有依賴誤存為空白。

### 依賴與排序

- Steam Required Items 是作者頁面的建議／依賴候選，永遠由使用者決定。
- `mod.info require=` 才視為硬依賴。
- 自動排序處理 `require`、`loadBefore`、`loadAfter`、`loadModBefore` 與 `loadModAfter`。
- 缺少的作者排序目標只提示，不會被誤判成必裝依賴。
- 發現循環規則時保留原相對順序並標示需人工確認。
- 同一 Workshop 內的一般版、特殊規則版或相容補丁逐項列出；互斥版本同時勾選時拒絕套用。
- root、common 與 42.x 變體會優先採用目前 Build 42 版本，不混入 B41 Mod ID。

### 地圖與重生點

只有同時包含 `map.info` 與實際 world cell 資料的模組才列為世界地圖，避免把語言包或純標註誤判成地圖。

- 在地圖清單勾選要加入 `Map=` 的地圖。
- 若偵測到 `spawnpoints.lua`，可另外選擇是否加入角色建立時的重生區域。
- 管理器只維護 `_spawnregions.lua` 中有明確標記的區塊，不覆蓋其他人工內容。

### 客戶端候選與白名單

PZ Dedicated Server 沒有一個可由管理器安全寫入、又能完整封鎖全部純客戶端修改的獨立「MOD 白名單」欄位。

「純客戶端候選」只表示本機掃描未發現 server/shared Lua、地圖或遊戲資料；這是審核提示，不會虛構伺服器設定。無法判定或包含素材時會保守標示「需人工確認」，Lua 校驗維持啟用。

### 寫入 INI

所有 MOD 變更先保留為待套用狀態。「套用勾選與順序」及「套用地圖與重生點」只更新 GUI；最後按「儲存全部設定」才同次寫入 `WorkshopItems=`、`Mods=`、`Map=` 與管理器控制的重生區域，並從磁碟讀回驗證。

## 6. 控制台、自動化與健康檢查

### 控制台指令

- 提供獨立「立即存檔」。
- 安全關服流程為倒數公告 → `save` → ZIP 備份 → `quit` → 等待程序結束。
- 手動輸入 `quit` 會取消本次工作階段的排程與自動重開，避免關服後又被排程拉起。
- 指令使用非阻塞背景佇列；PZ 停止讀取標準輸入時，GUI 仍可操作。

### 在線玩家

管理器使用官方 `players` 控制台指令，不使用 Build 42 已知可能有 Unicode 封包問題的 RCON 查詢。

- PZ 回報 `SERVER STARTED` 後才開始健康檢查。
- 啟動後立即查詢，之後依使用者設定的 1–1440 分鐘間隔更新，預設 30 分鐘。
- 查詢間隔在伺服器啟動後鎖定，停止後恢復可編輯。
- 只有收到實際玩家數才算健康；第一次逾時會在 30 秒後複查，連續兩次逾時顯示紅色警報並暫停全部自動化。
- 同一時間只允許一個短時間查詢，避免指令互相干擾。

### CLI 卡死

LOG 持續輸出不代表遊戲主迴圈正常。若 `players` 無回應，管理器不會自動殺程序，而是保持 PZ、停止自動化並要求人工確認。

只有使用者確認無法恢復後，才能從警報操作「強制終止卡死程序」。此操作只終止管理器本次啟動的 PZ 程序樹，不會關閉 Windows VM 或其他程序。

啟動批次檔意外停止或停在 `PAUSE` 時，管理器會回收外殼程序，不依賴 UI 執行緒等待。

### 定時重啟

- 可設定重啟時間、自訂倒數公告及重啟前 ZIP 備份。
- 歡迎訊息可附加下一次定時重啟時間；MOD 更新重啟不計入。
- 伺服器運行中仍可關閉定時重啟、Workshop 檢查與公告，也可一次停止全部自動化。

### Workshop 更新

Workshop 更新檢查與等待玩家離線公告預設皆開啟，並有獨立間隔；預設分別為 5 與 30 分鐘。公告是更新檢查的子選項，關閉檢查時也會停用公告。

- 以 Valve Workshop 公開 API 的 `time_updated` 對照本機 `appworkshop_108600.acf`。
- 缺少可靠本機紀錄時不猜測更新，也不觸發重啟。
- 偵測更新且無玩家時走安全存檔、關服與重啟流程。
- 有玩家時立即公告一次，之後依自訂間隔公告；直到確認無人才重啟更新。
- 自動重啟不重新套用 GUI，啟動阻擋只寫入控制台與狀態，不等待人工關閉提示窗。

## 7. 診斷、記錄與隱私

「診斷與記錄」會在啟動及進入頁面時刷新環境摘要，檢查：

- SteamCMD 與 PZ 啟動器。
- 目前 INI 與 Sandbox 路徑、版本及編碼。
- 連接埠、磁碟空間與記憶體。
- PZ 資料目錄的寫入能力。

診斷是唯讀操作，不會建立缺少的 PZ 資料目錄或修改設定檔。

GUI 記錄按日保存，可搜尋、暫停自動捲動及自訂保留天數。隱私清理診斷包不包含密碼、Steam64 ID、IP、MAC、Windows 使用者路徑或 MOD 訂閱清單。

## 8. 設定與資料位置

- Manager 設定預設：EXE 旁的 `manager-settings.json`。
- Manager 設定備選：`%LOCALAPPDATA%\PZServerManager\manager-settings.json`。
- 若兩處只有一處存在設定檔，會自動選擇該處；兩處皆無時預設使用 EXE 旁。
- PZ 設定：所選資料目錄下的 `Server`。
- 世界存檔：所選資料目錄下的 `Saves\Multiplayer`。
- ZIP 備份：所選資料目錄下的 `Backups`。
- GUI 記錄：優先使用 EXE 旁的 `Logs`；不可寫時改用 `%LOCALAPPDATA%\PZServerManager\Logs`。
- Workshop 快取：`%LOCALAPPDATA%\PZServerManager\workshop-metadata-cache.json`。

## 9. 語言與字型

- 儀表板可切換繁體中文與 English。
- `Languages` 內每個有效 UTF-8 JSON 都會成為語言選項。
- 複製 `en-US.json` 後修改 `code`、`displayName` 與翻譯值即可建立新語言。
- `{0}`、`{1}`、`<LINE>` 等佔位符不可移除或改名；缺少翻譯時安全回退繁中。
- 伺服器原始輸出、設定值、玩家名稱與模組名稱不翻譯，避免改變實際資料。
- 內建 Noto Sans TC、Noto Serif TC、霞鶩文楷 TC，另可使用系統微軟正黑體。

## 10. 常見問題

### 看不到主要介面

先依前置導引完成 SteamCMD 與 PZ Dedicated Server 安裝。未完成前，其他設定頁會刻意隱藏。

### 無法儲存設定

先按「從目前檔案讀取 GUI」。若檔案被其他程式修改、Sandbox 版本不符、值超出範圍或文字已損壞，管理器會拒絕覆寫。

### 文字亂碼

選擇正確編碼後重新讀取；切換編碼不會寫檔。有效 UTF-8 會拒絕以 Big5 誤讀。只有確認文字曾被錯誤轉碼且仍可逆時才使用修復模式。

### `Roles.getDefaultForUser() is null`

確認管理員密碼不為空白，使用新的設定檔名稱測試乾淨 B42 資料庫，再以空白 `WorkshopItems`／`Mods` 測試純原版。管理器不會自動刪除既有資料庫。

### 玩家斷線後伺服器卡住

若快速斷線／重連後反覆卡住，可暫時將 `PauseEmpty` 設為 `false` 觀察。管理器只顯示風險提示，不會擅自改變官方預設或既有值。

### 設定損壞

使用「檢查／還原設定備份」還原 `.manager-backup`。重大更新、MOD 變更或世界重建仍應使用 VM 快照及離線備份。

## 11. 授權與免責

原創程式碼依 [PolyForm Noncommercial License 1.0.0](LICENSE) 提供。非官方定位、第三方權利與風險說明請見 [免責聲明](DISCLAIMER.md)。
